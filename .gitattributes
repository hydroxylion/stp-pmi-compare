# 批处理必须 CRLF：cmd.exe 遇到 LF 换行时，含 if (...) / for 块的批处理会整块
# 错位解析，表现为「每一行都报不是内部或外部命令」。详见 README「诊断入口」。
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 其余显式钉成 LF：仓库里统一 LF，工作区不要被 autocrlf 来回改，
# 否则每次 git 操作都刷「LF will be replaced by CRLF」的警告。
.gitattributes text eol=lf
*.py text eol=lf
*.md text eol=lf
*.txt text eol=lf
